# =====================================================
# 23:47 Frontend — Variables de entorno
# Copia como `.env` y ajusta para cada entorno.
# SOLO incluir variables públicas (prefijo VITE_).
# NUNCA pongas secrets/keys privadas en el frontend.
# =====================================================

# URL del API backend
VITE_API_URL=http://localhost:4000

# URL del WebSocket (Socket.IO)
VITE_SOCKET_URL=http://localhost:4000
